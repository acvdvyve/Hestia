<h1>Albums</h1>

<% @album.each do |album| %>
    <tr>
      <td><%= link_to album.title, album_photos_path(album) %></td>
      <td><%= link_to 'Edit', album_path(album) %></td>
    </tr>
  <% end %>
</br>
  <%= link_to 'New', new_album_path %>
